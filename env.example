# Variables de entorno para Railway - Copiar a .env en desarrollo local
# ==========================================

# Puerto de la aplicación (Railway lo asignará automáticamente)
PORT=8080

# Configuración de Base de Datos MySQL
# Formato: jdbc:mysql://host:puerto/nombreBaseDatos?useSSL=false&serverTimezone=UTC
DATABASE_URL=jdbc:mysql://localhost:3306/paginascitasweb?useSSL=false&serverTimezone=UTC
DATABASE_USER=root
DATABASE_PASSWORD=admin

# Mostrar queries SQL en logs
SHOW_SQL=true

# Configuración de Hibernate
# create-drop: Elimina y crea la BD en cada inicio (desarrollo)
# update: Actualiza la BD sin perder datos (producción)
# validate: Solo valida (más seguro en producción)
HIBERNATE_DDL_AUTO=update

# Configuración de Spring
SPRING_ENVIRONMENT=production
SPRING_JPA_SHOW_SQL=false
